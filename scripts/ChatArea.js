"use strict";

define(["Constants", "underscore"], function(Constants, _) {
    var obtain = function() {
        var ChatArea = function() {
            this.el = $("#chat-area");

        };
    }();

    return {obtain: obtain};
});
